# 변경이력 (Change History)

> F28069 Pack BMS 펌웨어 변경이력. 형식: **NO · 날짜 · 내용**
> 신규 항목은 표 하단에 번호를 이어서 추가한다.

## 저장소 / 유지보수 이력

| NO | 날짜 | 내용 |
|----|------|------|
| 1 | 2026-06-02 | Git 저장소 초기화. 현재 소스(CP949 원본)를 baseline으로 최초 커밋. 빌드 산출물(`Debug/`, `*.out`, `*.map` 등)은 `.gitignore`로 제외 |
| 2 | 2026-06-02 | 한글 주석 인코딩 CP949 → UTF-8(BOM 없음) 일괄 변환 (12개 파일). UTF-8 환경에서 주석 깨짐 해결. 한글은 전부 주석에만 존재하여 컴파일 무영향 |
| 3 | 2026-06-02 | 아키텍처 문서([ARCHITECTURE.md](ARCHITECTURE.md)) 및 본 변경이력 문서 작성 |
| 4 | 2026-06-04 | 과전류타임 Fault(`PackOcTime_Err`, `Cal80VSysFaultCheck`, [DSP28x_Project.c](../SysSoure/DSP28x_Project.c)) **기능 삭제(주석처리)** — 시간초과 누적 판정을 제거하고, 순시 과전류(`PackVCT_OV`) 절대값 임계를 506A → 500A 로 낮춰 대체. (`Product_Version` 20→25) |
| 5 | 2026-06-04 | 과전류 알람(`PackOC`, `Cal80VSysAlarmtCheck`, [DSP28x_Project.c:408](../SysSoure/DSP28x_Project.c#L408)) 임계 조정 — set 조건을 `Hyst_On`(strict `>`) → `>=` 비교로 변경하고, 임계 500A → **400A**, 해제 히스테리시스 456A → **300A** 로 하향 |

## 펌웨어 릴리스 이력 (빌드 산출물 기준 — 추정)

> 아래는 `Debug/` 빌드 산출물 파일명·날짜와 `parameter.h` 버전 매크로로 재구성한 것으로,
> 정확한 변경 내역은 담당자 확인 필요.

| NO | 날짜 | 내용 |
|----|------|------|
| 1 | 2025-10-18 | `F28069PackBMS251101_HD45F` 빌드 — HD45F 사양 |
| 2 | 2025-11-03 | `V3R0` 빌드 — 수소지게차 45kW 파워팩 보조배터리팩 (Product_Type=3, Version=20) |
| 3 | 2025-11-07 | `30KWFC_251107` 빌드 — 30kW 연료전지(FC) 사양 |

---

## 작성 가이드

새 변경이력을 추가할 때:

```
| <다음번호> | <YYYY-MM-DD> | <변경 내용 요약> |
```

- **NO**: 해당 표에서 이어지는 일련번호
- **날짜**: `YYYY-MM-DD`
- **내용**: 무엇을·왜 변경했는지 한 줄 요약 (필요 시 관련 커밋/파일 명시)
